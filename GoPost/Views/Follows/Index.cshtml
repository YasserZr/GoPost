@model IEnumerable<GoPost.Models.Follow>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Follower)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Followee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FollowedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Follower.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Followee.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FollowedAt)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
